#include <stdio.h>
#include <stdlib.h>

int main () {

}

List* MakeEmpty() {
  List *Ptrl;
  Ptrl = (List *)malloc(sizeof);
}

- ElementType FindKth(int K, List L)  访问 K 位置序列的相应的元素
- int Find(ElementType X, List L)     查找 X 第一次出现的位置
- void Insert(ElementType X, int i, List L)   i 之前插入一个元素
- void Delete(int i, List L)  删除指定位序 i 的元素
- int Length(List L)          线性表的长度