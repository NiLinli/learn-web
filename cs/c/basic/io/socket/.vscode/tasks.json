{
  "tasks": [
    {
      "label": "build",
      "type": "cppbuild",
      "command": "/usr/bin/gcc",
      "args": ["-fdiagnostics-color=always", "-g", "${workspaceRoot}/*.c", "-o", "${workspaceRoot}/a.out"],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": ["$gcc"],
      "group": "build",
      "detail": "调试器生成的任务。"
    },
    {
      "label": "echo-server-iom",
      "type": "shell",
      "command": "make",
      "args": ["echo-server-iom"]
    }
  ],
  "version": "2.0.0"
}
