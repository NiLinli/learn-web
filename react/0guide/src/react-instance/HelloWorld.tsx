import * as React from 'react';

export const element = <h1>Hello, world</h1>;

export const HelloWorld = () => element;

// 元素是构成 React 应用的最小单位
// 是构成 Component 的一部分
// React 元素是普通对象

// 通过 react-dom (浏览器平台) 确保 DOM 和 React 元素一致